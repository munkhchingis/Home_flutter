import 'package:flutter/material.dart';

class MovieSpecialCart extends StatelessWidget {
  const MovieSpecialCart({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
